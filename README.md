# node-api

Command line: 

npm install

npm install --save body-parser

npm install --save bcryptjs

Connect mongodb

REStful api post user:
json:
{
	"user": {
		"username" : "testUser",
		"email": "test@gmail.com",
		"pwd": "123"
	}
}

